{
  "name": "held-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for Held — a self-destructing diary. Adds a real background clock so entries expire and notify on schedule even when no browser is open.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "resend": "^6.16.0"
  }
}
